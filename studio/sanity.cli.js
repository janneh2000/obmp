// CommonJS so the Sanity CLI (loaded by raw Node) reads it without ESM/CJS errors.
// To switch projects, change projectId here AND in sanity.config.js.
module.exports = {
  api: {
    projectId: "8wm53mc6",
    dataset: "production",
  },
};
